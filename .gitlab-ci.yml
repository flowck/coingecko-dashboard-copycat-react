# Container image
image: node:alpine

# Stages
stages:
  - setup
  - build
  - test
  - deploy
  - release

# Cache node_modules
cache:
  paths:
    - node_modules/

# Setup jobs
install-dependencies:
  stage: setup
  tags:
    - changani
  script:
    - npm ci
  artifacts:
    paths:
      - node_modules/

# Test jobs
unit-tests:
  tags:
    - changani
  stage: test
  script:
    - npm run test

# Build jobs
build:
  tags:
    - changani
  stage: build
  script:
    - npm run build

# Release new version
# release-new-version:
#   stage: release
#   script: npm run deploy:prod
#   only:
#     refs:
#       - main

# Deploy
deploy-to-dev:
  tags:
    - changani
  stage: deploy
  script:
    - npm run deploy:dev
  only:
    refs:
      - develop

deploy-to-prod:
  tags:
    - changani
  stage: deploy
  script:
    - npm run deploy:prod
  only:
    refs:
      - main
